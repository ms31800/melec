#!/usr/bin/env python
# -*- coding: utf-8 -*-

from django.db import models


# Create your models here.
class eleve(models.Model):
    nom = models.CharField(max_length=30, verbose_name="Nom")
    prenom = models.CharField(max_length=30, verbose_name="Prénom")
    date_de_naissance = models.DateField(verbose_name="Date de naissance")

    def __unicode__(self):
        return u"{0} {1}".format(self.nom, self.prenom)


class objectif_seance(models.Model):


class preparation(models.Model):
    candidat = models.ForeignKey(eleve, related_name="candidat")

    def __unicode__(self):
        return u"{0}".format(self.candidat.nom)
